{
  "name": "websocket-client",
  "version": "1.0.0",
  "description": "Uses websocket to send data to websocket server which uses express.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Siddharth Mishra",
  "license": "MIT",
  "dependencies": {
    "ws": "^2.0.3"
  }
}
